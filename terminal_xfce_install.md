# INSTALAR Terminal-xfce4
```bash
sudo apt update
sudo apt install xfce4-terminal

#### Para hacerla predeterminada
sudo update-alternatives --config x-terminal-emulator

#### Luego seleccionar la terminal que se instaló
 /usr/bin/xfce4-terminal.wrapper
```